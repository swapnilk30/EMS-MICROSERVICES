spring:
  application:
    name: employee-service
    
    
  profiles:
    active:
    - prod
  
  
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      
      
server:
  port: 8081
      